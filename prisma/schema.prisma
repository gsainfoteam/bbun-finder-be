// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

// Looking for ways to speed up your queries, or scale easily with your serverless or edge functions?
// Try Prisma Accelerate: https://pris.ly/cli/accelerate-init

generator client {
  provider = "prisma-client"
  output   = "../generated/prisma"
}

datasource db {
  provider = "postgresql"
}

model User {
  uuid          String    @id @db.Uuid
  name          String //이름
  studentNumber String    @unique @map("student_number") //학번
  email         String    @unique //이메일
  createdAt     DateTime  @default(now()) @map("created_at")
  updatedAt     DateTime  @default(now()) @updatedAt @map("updated_at")
  deletedAt     DateTime? @map("deleted_at")

  isBbunRegistered Boolean @default(false) @map("is_bbun_registered") //BBun 등록 여부
  department       String? @map("department") //학과
  MBTI             String? @map("mbti") //MBTI
  instaId          String? @map("insta_id") //인스타 아이디
  description      String? @map("description") //짧은 소개

  profileImageUrl String? @map("profile_image_url") //프로필 이미지 URL

  @@map("user")
}
